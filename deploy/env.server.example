# API port inside the container. Uses 4000 by default if not provided.
PORT=4000

# PostgreSQL connection string reachable from the VPS or docker network.
DATABASE_URL=postgresql://postgres:postgres@db-host:5432/neurobox

# Secrets must be long, random strings. The server will refuse to start in production
# if JWT_SECRET or DATABASE_URL are missing.
JWT_SECRET=replace-with-strong-secret

# Comma-separated list of allowed origins for CORS, e.g. your HTTPS sites.
CORS_ORIGINS=https://app.example.com

# Optional: point SERVER_ENV to this file so server/scripts use the same values.
SERVER_ENV=.env.server


